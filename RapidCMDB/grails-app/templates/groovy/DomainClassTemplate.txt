<%
DEFAULT_IMPORTS.each{
%>
import ${it};
<%
}
%>
class ${model.model.name} ${model.model.parentModel == null?"implements com.ifountain.rcmdb.domain.generation.IGeneratedModel":"extends "+model.model.parentModel.name}
{
    ${AUTO_GENERATED_CODE}
}