package datasource;
import com.ifountain.core.domain.annotations.*;

class RCMDBDatasource extends BaseDatasource
{


    static searchable = {
        except = [];
    };
    static datasources = [:]

    

    static hasMany = [:]
    
    static constraints={
    
    }

    static mappedBy=[:]
    static belongsTo = []
    static transients = [];
   
}